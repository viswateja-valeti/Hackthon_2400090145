* {
  box-sizing: border-box;
}
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: #f4f7f9;
  margin: 0; padding: 0;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}
#app {
  background: white;
  width: 900px;
  max-width: 95vw;
  min-height: 600px;
  border-radius: 12px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.1);
  padding: 20px 30px;
}

/* Headings */
h1, h2, h3 {
  color: #333;
  margin-top: 0;
}

/* Buttons general */
button {
  cursor: pointer;
  border: none;
  border-radius: 6px;
  padding: 10px 16px;
  font-weight: 600;
  transition: background-color 0.3s ease;
}
button:hover {
  opacity: 0.9;
}

/* Form inputs */
input[type=email], input[type=password], input[type=text], select {
  width: 100%;
  padding: 10px 12px;
  margin: 6px 0 15px;
  border-radius: 6px;
  border: 1px solid #bbb;
  font-size: 1rem;
}

/* Form group spacing */
.form-group {
  margin-bottom: 12px;
}

/* Login container */
.login-container {
  max-width: 400px;
  margin: 40px auto;
  text-align: center;
}

/* Error and success messages */
.error {
  color: #d63447;
  margin-bottom: 12px;
}
.success {
  color: #2d9cdb;
  margin-bottom: 12px;
}

/* Hint text under login */
.hint {
  margin-top: 10px;
  font-size: 0.9rem;
  color: #666;
}

/* Buttons: primary, add, remove, small */
.btn-primary {
  background-color: #2d9cdb;
  color: white;
  width: 100%;
  font-size: 1rem;
}
.btn-add {
  background-color: #27ae60;
  color: white;
}
.btn-remove {
  background-color: #d63447;
  color: white;
}
.btn-small {
  padding: 6px 10px;
  font-size: 0.85rem;
  border-radius: 5px;
}

/* NAV */
nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}
/* Dashboard main container */
.dashboard {
  background: #fff;
  border-radius: 16px;
  box-shadow: 0 12px 30px rgba(0,0,0,0.12);
  padding: 25px 35px;
  max-width: 900px;
  margin: 20px auto;
  min-height: 650px;
  display: flex;
  flex-direction: column;
}

/* Navigation bar inside dashboard */
.dashboard nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30px;
  border-bottom: 2px solid #e0e6f8;
  padding-bottom: 12px;
}
.dashboard nav h2 {
  font-size: 1.9rem;
  color: #3c3c72;
  font-weight: 700;
  letter-spacing: 1px;
}
.dashboard nav button {
  background: #764ba2;
  color: white;
  padding: 10px 18px;
  border-radius: 30px;
  font-weight: 700;
  font-size: 1rem;
  transition: background-color 0.3s ease;
  box-shadow: 0 5px 14px rgba(118, 75, 162, 0.5);
  border: none;
}
.dashboard nav button:hover {
  background: #5e3e9d;
  box-shadow: 0 7px 20px rgba(94, 62, 157, 0.7);
  transform: translateY(-2px);
  cursor: pointer;
}

/* Content layout */
.dashboard .content-section {
  display: flex;
  gap: 40px;
  flex-wrap: wrap;
  justify-content: space-between;
}

/* Left and right panels */
.left-panel, .right-panel {
  flex: 1 1 45%;
  background: #f9f9ff;
  border-radius: 14px;
  padding: 25px 30px;
  box-shadow: inset 0 0 15px #e0e6f8;
  max-height: 540px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
}

/* Headings inside panels */
.left-panel h3, .right-panel h3 {
  font-size: 1.4rem;
  color: #4a3d72;
  margin-bottom: 20px;
  border-bottom: 2px solid #764ba2;
  padding-bottom: 8px;
  font-weight: 700;
  letter-spacing: 0.04em;
}

/* Tables */
table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0 10px;
  font-size: 0.95rem;
  color: #444;
}

/* Table headers */
th {
  background: #764ba2;
  color: white;
  padding: 12px 15px;
  font-weight: 700;
  text-align: left;
  border-radius: 8px 8px 0 0;
  letter-spacing: 0.03em;
}

/* Table body rows */
tbody tr {
  background: white;
  box-shadow: 0 2px 5px rgba(118, 75, 162, 0.15);
  border-radius: 12px;
  transition: transform 0.2s ease;
}
tbody tr:hover {
  transform: translateY(-3px);
  box-shadow: 0 5px 15px rgba(118, 75, 162, 0.3);
}

/* Table cells */
td {
  padding: 12px 15px;
  vertical-align: middle;
  color: #555;
  font-weight: 500;
}

/* Adjust column widths for clarity */
#course-table th:nth-child(1),
#user-course-table th:nth-child(1),
#user-schedule-table th:nth-child(1),
#registrations-table th:nth-child(2) {
  width: 15%;
  min-width: 80px;
}
#course-table th:nth-child(2),
#user-course-table th:nth-child(2),
#user-schedule-table th:nth-child(2),
#registrations-table th:nth-child(3) {
  width: 40%;
}
#course-table th:nth-child(3),
#user-course-table th:nth-child(3),
#user-schedule-table th:nth-child(3),
#registrations-table th:nth-child(4) {
  width: 25%;
}
#course-table th:nth-child(4),
#user-course-table th:nth-child(4) {
  width: 10%;
  text-align: center;
}
#course-table th:nth-child(5),
#registrations-table th:nth-child(5),
#user-course-table th:nth-child(5),
#user-schedule-table th:nth-child(4) {
  width: 10%;
  text-align: center;
}

/* Buttons inside tables */
button.btn-add, button.btn-remove {
  padding: 6px 12px;
  font-size: 0.85rem;
  border-radius: 6px;
  font-weight: 600;
  transition: background-color 0.25s ease;
  box-shadow: 0 3px 8px rgba(0,0,0,0.12);
}

button.btn-add {
  background-color: #27ae60;
  color: white;
}
button.btn-add:hover {
  background-color: #1e8449;
  box-shadow: 0 4px 10px rgba(30, 132, 73, 0.75);
  cursor: pointer;
}

button.btn-remove {
  background-color: #d63447;
  color: white;
}
button.btn-remove:hover {
  background-color: #a82a37;
  box-shadow: 0 4px 10px rgba(168, 42, 55, 0.75);
  cursor: pointer;
}

/* Forms in admin panel */
.admin-form {
  margin-top: 20px;
  background: #fff;
  padding: 20px 25px;
  border-radius: 12px;
  box-shadow: 0 5px 20px rgba(118, 75, 162, 0.15);
}

.admin-form label {
  font-weight: 700;
  color: #5e3e9d;
  margin-bottom: 8px;
  display: block;
  font-size: 1rem;
}

.admin-form input[type=text],
.admin-form input[type=number],
.admin-form select {
  width: 100%;
  padding: 12px 15px;
  border-radius: 10px;
  border: 1.5px solid #bfb8e8;
  font-size: 1rem;
  color: #4b3f72;
  box-shadow: inset 2px 2px 6px #e6e2f8, inset -2px -2px 6px #fff;
  font-weight: 600;
  margin-bottom: 18px;
  transition: border-color 0.3s ease;
}

.admin-form input[type=text]:focus,
.admin-form input[type=number]:focus,
.admin-form select:focus {
  border-color: #764ba2;
  outline: none;
  box-shadow: 0 0 8px rgba(118, 75, 162, 0.6);
}

/* Form button */
.admin-form button.btn-add {
  width: 100%;
  padding: 14px 0;
  font-size: 1.1rem;
  border-radius: 30px;
  box-shadow: 0 7px 20px rgba(39, 174, 96, 0.6);
}

/* Scrollbar styling */
.left-panel::-webkit-scrollbar,
.right-panel::-webkit-scrollbar {
  width: 8px;
}

.left-panel::-webkit-scrollbar-thumb,
.right-panel::-webkit-scrollbar-thumb {
  background-color: #b3a9d8;
  border-radius: 5px;
}

.left-panel::-webkit-scrollbar-track,
.right-panel::-webkit-scrollbar-track {
  background: #f0eff8;
}

/* Conflict message styling */
.conflict-msg,
#conflicts-list {
  margin-top: 15px;
  font-weight: 700;
  color: #d63447;
  font-size: 1.05rem;
  line-height: 1.3;
  min-height: 60px;
}

/* Responsive tweaks for smaller screens */
@media (max-width: 900px) {
  .dashboard .content-section {
    flex-direction: column;
    gap: 30px;
  }
  .left-panel, .right-panel {
    max-height: none;
    flex: 1 1 100%;
  }
}
